import java.util.*;

public class Main {
    public static void main(String[] args) {
        int[] array = {5, 4, 6, 3, 9};

        System.out.println(findMaxMin.getMax(array, array.length - 1));
        System.out.println(findMaxMin.getMin(array, array.length - 1));
    }
}
